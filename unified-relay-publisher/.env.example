# Unified Relay Publisher Configuration Example

# Service identification
SERVICE_NAME=unified-relay

# Database configuration - Choose one type
DATABASE_TYPE=mongo  # or 'postgres'

# MongoDB configuration (when DATABASE_TYPE=mongo)
MONGO_URI=mongodb://localhost:27017/ecommerce

# PostgreSQL configuration (when DATABASE_TYPE=postgres)
POSTGRES_URI=postgresql://postgres:postgres@localhost:5432/postgres

# RabbitMQ configuration
RABBITMQ_URI=amqp://admin:admin123@localhost:5672

# Redis Streams configuration
REDIS_URI=redis://localhost:6379

# Routing configuration file path
ROUTING_CONFIG_PATH=/app/config/routing.yml

# Polling configuration
POLL_INTERVAL_MS=500
BATCH_SIZE=100

# Cleanup configuration (PostgreSQL only, MongoDB uses TTL index)
CLEANUP_INTERVAL_HOURS=12
RETENTION_HOURS=24
